repos:
  - repo: local
    hooks:
      - id: requirements-lint
        name: Requirements Register Validator
        entry: python requirements_lint.py --no-colour
        language: system
        files: ^(REQUIREMENTS\.md|COMPLIANCE\.md|\.requirements-lint\.json)$
        pass_filenames: false
